name: Legacy Pages deploy (desativado)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "Este workflow foi desativado."
          echo "A publicacao oficial agora ocorre no index.yml, acionada pelo commit final de addons.xml."
